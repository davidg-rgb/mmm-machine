# API Base URL (default: /api, proxied by Vite in dev mode)
# For production Docker, the nginx proxy handles /api routing
# For direct API access without proxy, set to full URL:
# VITE_API_URL=http://localhost:8000/api
VITE_API_URL=/api
